import React from 'react'
import PropTypes from 'prop-types'

const WordCardFooter = props => {
  return <div>facebook createbe by on</div>
}

WordCardFooter.propTypes = {}

export default WordCardFooter
